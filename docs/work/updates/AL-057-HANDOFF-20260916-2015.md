---
ticket: AL-057
type: HANDOFF
status: IN_REVIEW
branch: codex/student-course-experience
commit: pending
product_impact: Adds a separate student class-library surface for the existing approval-gated API.
demo_impact: Makes an enabled synthetic/public-content pilot usable after Google OAuth configuration; no real-course activation is implied.
data_impact: local-only
checks: make check (805 passed, 28 skipped); focused Google sign-in, classroom-client, class-assistant, task, and shell tests (19 passed)
remaining_risk: Server feature gate remains off; enrollment selection and tasks are local convenience state, while server membership remains authoritative.
---

The client sends a Google ID token only when redeeming an invite or asking the
selected class. It displays only the returned cited response or decline and
does not keep a question/answer transcript. Confirmed tasks use local storage
only and can be removed locally.
