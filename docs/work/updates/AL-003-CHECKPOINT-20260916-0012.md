---
ticket: AL-003
type: CHECKPOINT
status: IN_REVIEW
branch: accesslens-extension-ar-pivot
commit: fd63b67
product_impact: none; preserves the current instructor-authorized live visual-meaning MVP
demo_impact: Stop now accurately freezes a student view without calling an open session ended
data_impact: temporary-service
checks: make check passed; 61 Access Pack, 24 relay, 5 delivery-board, 266 extension, and 53 live-session tests
remaining_risk: the additive shared contract needs a second review and the deployed relay must be rebuilt and deployed before it is claimed as live-AWS evidence
---

Implemented the T-21 lifecycle correction across the Zod and JSON Schemas,
Python reference validator, event simulator/fixture, instructor controller,
student state and rendered status, relay latest-state behavior, and relay rules.
`capture.stopped` is base-only and instructor-only, preserves no raw media or
student data, does not close the relay session, and is retained only as the
latest temporary semantic state. `session.ended` remains the terminal event.

The full repository gate passed after the change. This checkpoint is
intentionally IN_REVIEW: a second reviewer must inspect the shared contract
before merge, and deployment is a separate authorized operation.
