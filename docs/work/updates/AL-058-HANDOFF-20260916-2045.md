---
ticket: AL-058
type: HANDOFF
status: IN_REVIEW
branch: codex/class-library-deletion-jobs
commit: pending
product_impact: Profile deletion archives immediately and returns a durable, owner-only asynchronous purge job.
demo_impact: Supports synthetic/public pilot cleanup without enabling the class assistant or real-course data.
data_impact: persistent-instructor-content
checks: focused deletion/API/OpenAPI tests; root and infra typechecks; CDK synth; make check pending
remaining_risk: The job table is deployment-durable but the current source bucket is still shared rather than physically per instructor; malware scanning remains a separate control.
---

The request Lambda only archives and queues work. The separately privileged
worker deletes the class document prefix, chunk manifests, vectors, profile,
invites, memberships, and facts. It stores no worker exception text, and an
instructor can retry a failed dispatch/purge by issuing delete again.
