---
ticket: AL-001
type: CHECKPOINT
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the narrow QA commit is created
product_impact: none; existing explicit capture starts from the same instructor action with corrected browser-activation and consent ordering
demo_impact: source-order regression coverage is ready for the Windows tab/window/display matrix, not a claim of physical capture proof
data_impact: none
checks: npm run typecheck; focused capture-controller suite (25); full make check (61 Access Pack, 24 relay, 5 delivery-board, 288 extension, 53 live-session)
remaining_risk: permission denial, tab/window/display selection, source closure, matching, correction, pause/stop/restart, and multi-device delivery require physical-browser evidence
---

The controller begins `requestStream()` before awaiting session creation, retaining
the direct Start-click activation browsers require for display capture. It awaits
the instructor's grant before creating the temporary session, so a denied chooser
does not leave a session to close. The focused tests prove this consent ordering
and prove a granted stream is stopped if session creation fails. No capture data,
event shape, matcher, storage, relay, or student data behavior changes.
