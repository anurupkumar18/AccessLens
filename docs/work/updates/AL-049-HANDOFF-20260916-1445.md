---
ticket: AL-049
type: HANDOFF
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the AL-049 feature commit is created
product_impact: instructor can send a live caption; student can see a rolling transcript, opt-out available
demo_impact: closes the "live captions and transcript" P0 item from docs/ADVANCED_FEATURES.md at the code level
data_impact: temporary-service
checks: 12 new tests across 4 files, full 338-test suite, typecheck, make check; dist/ preview visually confirmed the student toggle with no console errors
remaining_risk: instructor-side form needs real getDisplayMedia QA, same as AL-001/AL-002; screen-reader/live-region QA for the caption track is open
---

AL-049 is handed to review and releases its claim. This wires AL-048's
`caption.appended` payload into the product: an instructor caption input
scoped to the current asset, and a bounded student transcript (5 entries,
`role="log"`) gated behind `captionsEnabled`. No contract or relay change --
AL-048 already shipped the payload shape this consumes. The instructor-side
form could not be exercised end-to-end in this sandboxed environment because
it needs a real `getDisplayMedia()` grant; unit coverage in
`captureController.test.ts` and `InstructorPanel.test.tsx` is the evidence
until real-device QA happens.
