---
ticket: AL-010
type: CHECKPOINT
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the narrow QA commit is created
product_impact: none; adds student-controlled presentational and requested-audio controls to the existing renderer
demo_impact: makes local accessibility adjustments demonstrable without disclosure to instructor or relay
data_impact: local-only
checks: npm run typecheck; focused preferences, StudentExperience, and AudioView tests (33); full make check (61 Access Pack, 24 relay, 5 delivery-board, 285 extension, 53 live-session)
remaining_risk: unpacked-extension, Windows, keyboard, screen-reader, and reload evidence remain operator QA; no live multi-device or reviewer evidence is implied
---

AL-010 adds strict local preference fields for neutral font choices, line
spacing, reading width, higher contrast, and speech rate. Existing saved
preferences missing these additions parse with safe defaults. The student screen
uses native labeled controls, passes the values only to local presentation, and
sets `SpeechSynthesisUtterance.rate` only after a student clicks Play.

The focused tests cover legacy defaults, local-storage round-trip, invalid values,
keyboard-accessible control changes, requested-only speech rate, and rejection of
a preference-shaped `LiveEvent`. No `LiveEvent`, `SessionClient`, capture, relay,
AWS, identity, analytics, or retention behavior changed.
