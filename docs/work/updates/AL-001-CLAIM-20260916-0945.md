---
ticket: AL-001
type: CLAIM
status: IN_PROGRESS
branch: codex/demo-proof-sprint-qa
commit: 2427b1d
product_impact: none; repairs the timing of the existing explicit browser capture request
demo_impact: removes a known source-order risk before the Windows tab/window/display matrix
data_impact: none
checks: existing capture-controller tests green before change; focused regression and full gate pending
remaining_risk: physical browser permission behavior, source selection, and multiple-device delivery remain operator-only evidence
---

An upstream change on `origin/master` categorized the risk: capture was requested
after `await client.create()`, so a remote session delay could consume the Start
click's transient user activation. This claim adopts only the narrow controller
ordering fix and test. The new `T-34` row records the physical-device evidence
still required before calling the matrix complete.
