---
ticket: AL-056
type: HANDOFF
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending
product_impact: Adds an approval-gated cited class-library path while leaving the live semantic relay separate.
demo_impact: A synthetic/public PDF proof can be deployed only after an explicit feature-flag context change; it is disabled by default.
data_impact: persistent-instructor-content
checks: npm run typecheck; cd infra and npm run typecheck; 34 focused Vitest assertions across nine files; generated OpenAPI drift test
remaining_risk: Real-course activation is not approved; a physical per-instructor S3 bucket and durable asynchronous deletion-status worker remain follow-up infrastructure work before production activation.
---

The implementation enforces server-side class membership before facts, retrieval,
or a model receive a request; returns citations or declines; caps student quotes;
and keeps questions, answers, tasks, and student preferences out of persistent
service storage. PDF uploads are staged in quarantine and pass size/signature/
encryption guards before a private class prefix is indexed. Automatic schedule
and recap candidates are deterministic cited drafts and can never publish
themselves.

The model path is feature-gated off. No real course data, Canvas data, student
work, answer keys, raw media, or live-relay data is activated by this change.
