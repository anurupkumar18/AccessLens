---
ticket: AL-041
type: CHECKPOINT
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the AL-041 feature commit is created
product_impact: reviewed focus indication only
demo_impact: semantic pointer implementation is ready for unpacked-extension QA; no device or relay claim
contract_impact: existing optional region.changed.pointer field now carried end to end; no schema change
data_impact: temporary-service
checks: focused 48-test pointer suite and full extension typecheck/test/build (294 tests)
remaining_risk: visual/browser and multi-device confirmation remain operator work
---

The instructor's existing indication control now emits the reviewed region's
normalized center as `region.changed.pointer`. Student live state preserves the
validated pointer and Focus rendering shows an explicit visual marker only when
that event field exists. No raw cursor position or capture content is sampled,
stored, or transmitted.
