---
ticket: AL-042
type: CHECKPOINT
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the AL-042 feature commit is created
product_impact: none; fail-closed authoring seam only
demo_impact: makes the disabled model boundary inspectable without a claim that AI is running
data_impact: none
checks: focused Bedrock gateway tests (6) and extension typecheck
remaining_risk: no approved provider, review policy, source-content boundary, or model invocation exists
---

`createBedrockGateway()` returns only a disabled gateway. Its strict request
schemas permit reviewed identifiers but reject raw screen data, student identity,
and course text; both operations then raise a clear disabled error. The module
contains no SDK import, credential lookup, environment activation, network client,
or model output path.
