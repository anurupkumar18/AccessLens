---
ticket: AL-053
type: HANDOFF
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the AL-053 feature commit is created
product_impact: window and full-screen shares can now match a reviewed slide; previously only tab shares synced at all
demo_impact: removes a real blocker for AL-001's window/display capture cases
data_impact: none
checks: 354 extension tests, typecheck, make check reproduced green twice after fixing one flaky timeout under parallel load
remaining_risk: not yet run against a real getDisplayMedia() window or screen -- folds into AL-001's existing device matrix
---

AL-053 is handed to review and releases its claim. Cherry-picked from
`ui/blacksmith-revamp` (Omar Rizwan, `8fde5aa`) as part of cross-branch
integration this session. Conflicts in `InstructorPanel.test.tsx` were
additive (two independent new test cases) and resolved by keeping both;
`captureController.ts`, `captureController.test.ts`, and `InstructorPanel.tsx`
auto-merged cleanly against this session's caption-form and session-end-on-
unmount additions. Found and fixed one genuinely flaky test
(`locate.test.ts`'s whole-screen search) that timed out under `make check`'s
full parallel worker load but passed reliably alone -- gave it an explicit
20s timeout so the gate isn't intermittently red.
