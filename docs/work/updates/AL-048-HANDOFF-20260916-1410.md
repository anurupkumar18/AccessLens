---
ticket: AL-048
type: HANDOFF
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the AL-048 feature commit is created
product_impact: contract-only; no UI wired to it yet
demo_impact: closes T-16 so a caption event can carry a caption; not a claim that captions are shown anywhere in the product today
data_impact: temporary-service
checks: 326 extension tests, 62 pack Python tests, full make check
remaining_risk: none for the contract itself; the UI wiring (instructor input, student display) is a separate follow-on slice
---

AL-048 is handed to review and releases its claim. `caption.appended` now
requires `assetId` and `caption: {text, isFinal}` in both the Zod contract and
its JSON Schema mirror, closing both gaps `check_contract_conformance.py` had
tracked since Part 5 first wrote the `captions` fixture. No relay code change
was needed — `services/live-session/src/rules.ts` and the Python reference
validator already allowlisted the `caption` field name in anticipation of
this closing. `docs/PART5_CONTRACT_CONFORMANCE.md` section 3 is marked
Closed. The next slice is the UI: an instructor caption input and a student
caption/transcript display behind the existing, currently-unused
`captionsEnabled` preference.
