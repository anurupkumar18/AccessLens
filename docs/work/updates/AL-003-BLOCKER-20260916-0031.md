---
ticket: AL-003
type: BLOCKER
status: IN_REVIEW
branch: master
commit: cd52f6e
product_impact: none; preserves the current extension-first live visual-meaning MVP
demo_impact: prevents an unreviewed or undeployed lifecycle change from being described as live-AWS proof
data_impact: temporary-service
checks: make check passed; deployed integration test passed; 30-event bench passed ordered delivery but endpoint rejected capture.stopped as event-type-not-allowlisted; local machine has neither aws CLI nor configured AWS credentials
remaining_risk: an independent shared-contract review and authorized relay deployment remain required
---

The current release passes the full local gate and contains the tested
`capture.stopped` lifecycle behavior. The existing endpoint passed the standard
19-event integration test and the new bench delivered 30/30 ordered events to
each anonymous student client, but it rejected `capture.stopped` with
`event-type-not-allowlisted`; the endpoint is therefore stale for AL-003.
Deployment was not attempted because this machine has no AWS CLI installation,
AWS profile directory, or credential environment. The deployment and integration
commands are now captured in `docs/DEMO_PROOF_SPRINT.md` for an authorized
operator.
