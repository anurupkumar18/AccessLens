---
ticket: AL-051
type: HANDOFF
status: IN_REVIEW
branch: codex/demo-proof-sprint-qa
commit: pending commit; recorded before the AL-051 feature commit is created
product_impact: student views no longer go stale forever when the instructor leaves mid-share
demo_impact: fixes a real trust bug RL-016 had recorded as a known limitation, not just documented it
data_impact: temporary-service
checks: 342 extension tests (3 new), typecheck, make check
remaining_risk: real-device confirmation that a second real device sees the ended message promptly folds into the existing device QA matrix
---

AL-051 is handed to review and releases its claim. RL-016 called this "fine
for a demo, wrong for a product" -- it was actually load-bearing: an existing
`App.test.tsx` test was asserting on the buggy stale-content behavior to pass
at all, which is how it surfaced. `InstructorPanel`'s unmount cleanup now
ends the session properly (`capture.stopped` then `session.ended`) before
releasing local media, whenever the panel unmounts for any reason -- role
switch or lesson-pack switch. The affected test was restructured to use two
separate `App` instances sharing one client, which is also a more realistic
stand-in for two real devices than toggling role in a single tab.
