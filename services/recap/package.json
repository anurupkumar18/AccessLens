{
  "name": "@accesslens/recap",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AccessLens catch-up service — \"what did I miss\" recaps from the live event timeline.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.mjs --external:@aws-sdk/*",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
