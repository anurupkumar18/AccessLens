import {describe,it,expect} from 'vitest'; import {AccessPackSchema,LiveEventSchema} from './contracts'; import {validPack,validEvent,invalidEvent} from './fixtures';
describe('AccessLens contracts',()=>{it('accepts valid fixtures',()=>{expect(AccessPackSchema.safeParse(validPack).success).toBe(true);expect(LiveEventSchema.safeParse(validEvent).success).toBe(true)});it('rejects malformed event bounds',()=>expect(LiveEventSchema.safeParse(invalidEvent).success).toBe(false));it('rejects prohibited raw media',()=>expect(LiveEventSchema.safeParse({...validEvent,rawFrame:'x'}).success).toBe(false))});
